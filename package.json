{
  "name": "vizui.components",
  "version": "2.0.1",
  "description": "Components library for VizUI",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dependencies": "npx dep install",
    "compile": "rm -rf dist && npx tsc",
    "build": "npm run dependencies && npm run compile",
    "dev": "npx tsc --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "depflow": "^3.0.0-dev.10",
    "typescript": "^6.0.3"
  }
}
